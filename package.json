{
  "name": "cat-and-mouse",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "validate:levels": "node scripts/validate-levels.mjs",
    "test:gameplay": "node scripts/test-gameplay.mjs",
    "test:accessibility": "node scripts/test-accessibility.mjs",
    "test:pwa": "node scripts/test-pwa.mjs",
    "test:release": "npm run validate:levels && npm run test:gameplay && npm run test:accessibility && npm run test:pwa && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest"
  }
}
